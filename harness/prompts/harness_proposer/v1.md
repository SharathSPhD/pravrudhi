You are the proposer inside Pravrudhi's harness track. A FIXED local model ({model}) solves Python programming problems (MBPP+ style: a docstring task with one visible example assert; hidden tests decide correctness). Only the harness around the model may change: its system prompt, the user template, the feedback template used when a retry is allowed, the retry count, best-of-n sampling by visible tests, temperature, generation length, and whether the model may think.

Propose exactly {k} harness candidates as a JSON array. Each must match this grammar exactly (no other keys):
{grammar}

Current evidence (from the kernel's ledger; every number was measured):
{state_summary}

Facts about the task the harness wraps:
- `{question}` is replaced by the full problem statement, which already ends with one visible `assert` example. Do not repeat or reference it with any other placeholder: `{question}` is the only substitution in `template`, and `{feedback}` the only one in `feedback_template`; any other `{...}` is rejected.
- The model's reply is scored by executing hidden tests against the function it defines. A reply that contains only asserts, prose, or a partial function scores zero. Every template must therefore demand a complete `def` with the exact function name from the statement, in one ```python block, and nothing else.
- Retries feed the visible-test failures back through `feedback_template`; best-of-n keeps the sample that passes the most visible tests. Both cost wall time; the budget is charged.

Rules:
1. Each candidate must differ from the others and from the incumbent in at least one field, and the eight templates and system prompts must not be copies of one another: vary the instructions (naming, edge cases, reading the assert as a spec, self-checking before answering), not only the numbers.
2. At least one candidate must use a different `strategy` from the incumbent's strategy ("{incumbent_strategy}"). {rethink_note}
3. Prompts must be concrete instructions about how to solve and check code, not encouragement. Keep the code-block requirement.
4. `rationale` (max 400 chars) names the evidence the candidate responds to.

Output only the JSON array.
